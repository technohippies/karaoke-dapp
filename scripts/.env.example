# Scripts-specific configuration
# Copy this to .env and fill in your values

# Pinata (for uploading to IPFS)
PINATA_JWT=your_pinata_jwt_here

# API Keys for Lit Actions (these get encrypted and embedded)
DEEPGRAM_API_KEY=your_deepgram_api_key_here
OPENROUTER_API_KEY=your_openrouter_api_key_here

# PKP Details (filled after running mint-pkp-with-permissions.ts)
PKP_TOKEN_ID=
PKP_PUBLIC_KEY=
PKP_ETH_ADDRESS=

# Lit Action CID (filled after running upload-lit-action.ts)
LIT_ACTION_CID=

# Capacity Credits (filled after minting)
CAPACITY_CREDIT_NFT_ID=
CAPACITY_DELEGATION_AUTH_SIG=

# Note: PRIVATE_KEY is sourced from root .env